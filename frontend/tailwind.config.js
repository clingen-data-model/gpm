module.exports = {
    purge: [
        './src/**/*.vue'
    ],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
        container: {
            padding: '3rem'
        }
    },
    variants: {
        extend: {},
    },
    plugins: [],
}